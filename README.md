# Text-Retrieval


An Data or Text retriveal from Documents and images using AWS services and a flask application. 

**This application allows users to upload document images, automatically extracts text using AWS Textract, and stores all OCR(Optical Character Recognition) results in a single JSON file per user. The workflow is fully serverless and uses S3 + Lambda + Textract + Flask.**
